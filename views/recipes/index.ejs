<%- include('../partials/header')%>
<h1>Your Recipes</h1>
<table>
    <thead>
    <th>Title</th>
    <th>Ingredients</th>
    <th>Instructions</th>
    <th>Comments</th>
    </thead>
    <tbody>
    <% recipes.forEach(recipe => { %>
        <tr>
            <td><%= recipe.title %></td>
            <td><%= recipe.ingredients %></td>
            <td><%= recipe.instructions %></td>
            <td><%= recipe.comments %></td>
            <td><form action='/recipes/<%= recipe.id %>' method='POST>
                <button type='submit'>DELETE</form>button</form></td>
                <td><a href="/recipes/<%=recipe.id%>">DETAILS</a></td>
        </tr>
    <% }) %>
    </tbody>
</table>
<%- include('../partials/footer')%>